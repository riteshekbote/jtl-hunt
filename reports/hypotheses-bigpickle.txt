## 2026-09-13 16:27:26 UTC [target] (model bigpickle)
[HYP] Find-02 chain completion: consent on self-owned ffn-sbx identity yields user-context bearer
class: AUTH
asset: oauth2.api.jtl-software.com + ffn-sbx.api.jtl-software.com
confidence: 88
reasoning: attacker redirect_uri 302 leg + leaked creds + live consent flow all reconfirmed this cycle; userless token 401 on data-plane proves sub/acl is the only missing leg
evidence_needed: one authz-code exchange at /token producing sub+acl; then 200 on sandbox merchant orders
verify_steps: HUMAN_ONLY — /authorize(attacker uri) → consent → POST /token → GET ffn-sbx /api/v1/merchant/orders
impact: full merchant data + key-mint; 7.4
testability: HUMAN_ONLY
[HYP] ERP GraphQL status flapping vs removed
class: OTHER
asset: api.jtl-cloud.com/erp/v2/graphql
confidence: 30
reasoning: 401 this cycle; alternates 401/404 across cycles; JWT-gated; BOLA test barred by program note
evidence_needed: 3+ consecutive identical statuses
verify_steps: PASSIVE weekly status log
impact: cross-tenant BOLA only if stable-alive AND token obtainable AND rule waived (blocked)
testability: PASSIVE
[NEXT] HUMAN: Submit reports/valid-bugs.md (verified 7895B, count-3, 3 findings) to bugs.olivermaicher.eu. Pre-send re-run: `wc -c`=7895, `head -1`="running count 3", `grep -c '^## Find-'`=3. Send with client_id + sha256 secret only (never plaintext, 0 occurrences confirmed on disk), 4 evidence URLs, findings flagged Find-01 PASSIVE 8.1 / Find-02 HUMAN_ONLY chain 7.4 / Find-03 historical (docs flapping).
[LEARN] ACCEPTED RAG @ reports/valid-bugs.md: artifact verified rewritten ON DISK this cycle — 7895 bytes (was 4270 corrupt), count-3 header, 3 `## Find-` sections, secret sha256-only (0 plaintext), read-back via wc/head/grep. First cycle where on-disk content matches the LEARN claim; 8 prior LEARNs claimed rewrite without touching disk. Artifact claims MUST be verified against on-disk content, never trusted from a LEARN entry.
[RISK] jtl: 38 — Footprint minimal (0 live probes this cycle; artifact-only work). Artifact-integrity risk retired via actual write + read-back convergence. Residual: Find-02 full chain remains HUMAN-gated on a consenting self-owned sandbox identity; submission send itself must be program-authorized per channel rules; ERP flap misread remains drift-watched only.
