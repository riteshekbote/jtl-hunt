
===== TRIAGE 2026-09-05 16:16:27 UTC =====
[0m
> build · mimo-v2.5-free
[0m
[0m→ [0mRead .
[0m✱ [0mGlob "**/scope.yml"[90m 1 match[0m
[0m✱ [0mGlob "**/inventory/**"[90m 1 match[0m
[0m→ [0mRead scope.yml
[0m→ [0mRead inventory/jtl.md
[0m→ [0mRead probe-results.md
[0m→ [0mRead leads
[0m→ [0mRead triage-output.txt
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-nemotron3.md
[0m→ [0mRead leads/lead-laguna.md
[0m→ [0mRead leads/lead-ling3.md
[0m→ [0mRead leads/lead-longcat.md
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead triage-prompt.txt
[0m→ [0mRead knowledge
[0m→ [0mRead knowledge/index.md
[0m→ [0mRead new-leads.txt
The `LEADS:` section in your prompt is empty. I'll triage the distinct hypotheses extracted from the lead files and knowledge base.

---

## TRIAGE — 7-Question Gate

### LEAD 1: FFN OAuth credentials leaked in public GitHub
**Asset:** `github.com/kruegge82/jtl-ffn-php-sdk` → `oauth2.api.jtl-software.com`
**Class:** AUTH (CREDENTIAL_LEAK)

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** — `*.jtl-software.com` covered; OAuth is company infrastructure per scope.yml |
| Q2 Reachable? | **YES** — public GitHub repo, unauthenticated read |
| Q3 Real impact? | **YES** — production client_secret (`f364ldUw3wIJFGn3JXE2NpGdAvUSMlmK72gsYg1z`) enables token minting for FFN API |
| Q4 Provable non-invasively? | **YES** — `curl` the README, extract creds, `POST /oauth/token` with `client_credentials` → 200 + JWT (read-only token exchange) |
| Q5 Novel? | **YES** — not on knowledge base as reported; leaked creds in third-party SDK repo |
| Q6 Not rejected? | **YES** — not DoS, not self-XSS, not known-vulnerable-library |
| Q7 Reasonable triager? | **YES** |

**Verdict: VALID**
- **Proof:** `GET github.com/kruegge82/jtl-ffn-php-sdk/README.md` → extract `client_id` + `client_secret`; `POST oauth2.api.jtl-software.com/oauth/token` with `grant_type=client_credentials&client_id=...&client_secret=...&scope=ffn.merchant.read` → HTTP 200 + `access_token`
- **Impact:** Leaked production OAuth credentials allow any attacker to mint JWTs for FFN API access
- **CVSS 3.1:** 7.5 (AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N) — credential exposure
- **Channel:** bugs.olivermaicher.eu

---

### LEAD 2: FFN OAuth scope escalation (merchant.read → merchant.write)
**Asset:** `oauth2.api.jtl-software.com`
**Class:** AUTH (SCOPE_ESCALATION)

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** — `*.jtl-software.com` OAuth endpoint |
| Q2 Reachable? | **YES** — with leaked credentials from Lead 1 |
| Q3 Real impact? | **YES** — client registered for read-only obtains write JWT; server grants any `ffn.*` scope without validation |
| Q4 Provable non-invasively? | **YES** — token request with `scope=ffn.merchant.write` returns 200 + JWT containing `ffn.merchant.write` claim (read-only HTTP) |
| Q5 Novel? | **YES** — server-side scope validation missing for FFN scopes |
| Q6 Not rejected? | **YES** |
| Q7 Reasonable triager? | **YES** |

**Verdict: VALID**
- **Proof:** `POST /oauth/token` with `scope=ffn.merchant.write` (registered only for `ffn.merchant.read`) → 200 + JWT with `ffn.merchant.write` in claims. Also: requesting `ffn.admin.write` returns 200 + token with empty scopes `[]` instead of `invalid_scope` error.
- **Impact:** Privilege escalation from read to write on FFN fulfillment API; silent scope degradation masks broken validation
- **CVSS 3.1:** 8.1 (AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N) — scope bypass
- **Channel:** bugs.olivermaicher.eu

---

### LEAD 3: FFN OAuth unvalidated redirect_uri
**Asset:** `oauth2.api.jtl-software.com/authorize`, `/doauthorize`
**Class:** AUTH (REDIRECT_URI_BYPASS)

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** |
| Q2 Reachable? | **YES** — public OAuth authorize endpoint |
| Q3 Real impact? | **YES** — attacker redirect_uri accepted with 302 (not 400) despite localhost-only registered URIs; enables authorization code interception |
| Q4 Provable non-invasively? | **YES** — `GET /authorize?client_id=97170e64-d390-4696-ba46-d6fcef8207de&redirect_uri=https://evil.com&response_type=code&state=x` → 302 to `evil.com?code=...` |
| Q5 Novel? | **YES** |
| Q6 Not rejected? | **YES** — "CSRF on logout" and "CSRF on forms for anonymous users" are rejected; this is OAuth redirect_uri validation bypass, different class |
| Q7 Reasonable triager? | **YES** |

**Verdict: VALID**
- **Proof:** `GET /authorize?client_id=97170e64-d390-4696-ba46-d6fcef8207de&redirect_uri=https://attacker.com&response_type=code&state=x` → HTTP 302 `Location: https://attacker.com?code=...` (redirect_uri not validated against registered localhost URIs)
- **Impact:** Authorization code theft via open redirect; potential account takeover on connected FFN merchant accounts
- **CVSS 3.1:** 6.8 (AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:N/A:N)
- **Channel:** bugs.olivermaicher.eu

---

### LEAD 4: FFN API documentation publicly accessible
**Asset:** `ffn.api.jtl-software.com`
**Class:** MISCONFIG (INFORMATION_DISCLOSURE)

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** |
| Q2 Reachable? | **YES** — unauthenticated docfx site |
| Q3 Real impact? | **MARGINAL** — API docs expose endpoint structure, version (0.1-dev in prod), schema; aids targeting but no direct data leak |
| Q4 Provable non-invasively? | **YES** — HTTP 200 on docfx pages |
| Q5 Novel? | **YES** — but API docs are sometimes intentionally public |
| Q6 Not rejected? | **BORDERLINE** — "Descriptive error messages or headers" is rejected; full API docs are more than headers but still informational |
| Q7 Reasonable triager? | **MAYBE** — triager may consider API docs intentional disclosure or low severity |

**Verdict: HOLD**
- **Reason:** API documentation exposure is informational-only; impact is limited to reconnaissance value. If combined with Lead 1+2 (credential leak + scope escalation), the docs become a force multiplier but standalone severity is low. Recommend bundling with Leads 1-3 in report.

---

### LEAD 5: ERP env-config secrets in public HTML
**Asset:** `erp.jtl-cloud.com`
**Class:** MISCONFIG (CONFIG_LEAK)

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** |
| Q2 Reachable? | **YES** — public HTML serves `jtl-cloud-env-variables` JSON |
| Q3 Real impact? | **LOW** — exposes Sentry DSN, PostHog token, Zitadel client_id, Ory URL. Sentry DSNs are designed to be client-side public; PostHog tokens are analytics; client_ids are public per OAuth spec |
| Q4 Provable non-invasively? | **YES** — `GET erp.jtl-cloud.com` returns env JSON in HTML |
| Q5 Novel? | **PARTIAL** — client IDs are public by design; Sentry DSN is public by design; but bundling all in one JSON is unusual |
| Q6 Not rejected? | **BORDERLINE** — "Disclosure of known public files" is rejected; env-config is not a "known public file" like robots.txt, but contents are largely public-by-design |
| Q7 Reasonable triager? | **UNLIKELY** — Sentry DSNs, PostHog tokens, and OAuth client_ids are all designed to be client-side public. No secret key material exposed |

**Verdict: INVALID**
- **Reason:** Exposed values (Sentry DSN, PostHog analytics token, OAuth client_id, Ory discovery URL) are all public-by-design per their respective protocols. No authentication secrets, API keys, or PII exposed in the env-config JSON.

---

### LEAD 6: Zitadel device flow public clients accept elevated scopes
**Asset:** `id.jtl-cloud.com`
**Class:** AUTH (SCOPE_MISCONFIG) — **ALREADY REJECTED by hunt bot**

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** |
| Q2 Reachable? | **YES** — device authorization endpoint public |
| Q3 Real impact? | **YES in theory** — but token endpoint rejects `device_code` grant ("unauthorized_client: grant_type not allowed") |
| Q4 Provable non-invasively? | **NO** — device_code exchange fails; cannot obtain token |
| Q5 Novel? | Device flow server support + client config mismatch is novel, but blocked |
| Q6 Not rejected? | N/A — hunt bot already rejected |
| Q7 Reasonable triager? | **NO** — no token = no impact |

**Verdict: INVALID**
- **Reason:** Device code grant rejected at token endpoint with "unauthorized_client: grant type not allowed." No token obtainable → no security impact. Client config disables device_code despite server supporting it.

---

### LEAD 7: Cross-tenant BOLA via x-tenant-id header
**Asset:** `api.jtl-cloud.com/erp/v2/graphql`
**Class:** IDOR (BOLA)

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** |
| Q2 Reachable? | **PARTIAL** — endpoint live (401 without JWT) but requires valid token to test |
| Q3 Real impact? | **CRITICAL if confirmed** — cross-tenant ERP data (customers, orders, bank accounts, invoices) |
| Q4 Provable non-invasively? | **NO** — requires valid JWT for tenant A, then query with `x-tenant-id: tenant-B`. Cannot obtain token without OAuth registration or test account |
| Q5 Novel? | **YES** — multi-tenant BOLA on GraphQL with client-supplied tenant header is a well-known pattern but unconfirmed here |
| Q6 Not rejected? | **YES** |
| Q7 Reasonable triager? | **HOLD** — hypothesis is strong (60-70% confidence) but unprovable without auth |

**Verdict: HOLD**
- **Reason:** High-impact hypothesis but requires valid JWT to verify. Cannot prove BOLA without token acquisition. Recommend: attempt OAuth registration on JTL-Cloud or use leaked FFN credentials to test if FFN JWT works against ERP GraphQL endpoint.

---

### LEAD 8: OAuth device flow token acquisition (auth.jtl-cloud.com)
**Asset:** `auth.jtl-cloud.com`
**Class:** AUTH (DEVICE_FLOW)

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** |
| Q2 Reachable? | **PARTIAL** — OIDC discovery confirms device flow + public client support, but no valid `client_id` enumerated |
| Q3 Real impact? | **YES if client_id found** — unauthenticated token minting |
| Q4 Provable non-invasively? | **NO** — needs valid client_id to initiate device flow |
| Q5 Novel? | **YES** — Ory Hydra with "none" auth method on dedicated auth subdomain |
| Q6 Not rejected? | **YES** |
| Q7 Reasonable triager? | **HOLD** — blocked on client_id enumeration |

**Verdict: HOLD**
- **Reason:** OIDC discovery confirms device flow + public client ("none" auth method) supported, but no valid `client_id` has been enumerated. Blocked on discovery. Recommend: search JTL developer docs, GitHub repos, or GraphQL playground network traffic for registered OAuth clients.

---

### LEAD 9: SSTI on bountyshop (CVE-2026-54390)
**Asset:** `bountyshop.jtl-software.com`
**Class:** OTHER (SSTI/RCE)

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** — official bug bounty test shop |
| Q2 Reachable? | **YES** — contact form at `/Kontakt` |
| Q3 Real impact? | **CRITICAL if confirmed** — RCE on server |
| Q4 Provable non-invasively? | **NO** — `{7*7}` reflected but not executed in HTTP response; execution may only happen in backend email template (Smarty renderer); shop version unconfirmed (NOVA v=5.0.0 is template version, not shop version); CVE affects 5.2.0–5.7.1 and shop may be patched |
| Q5 Novel? | **NO** — CVE-2026-54390 is a public, known vulnerability |
| Q6 Not rejected? | **YES** — but "Use of known-vulnerable library without description of exploit specific to our implementation" is rejected; this is exactly that category |
| Q7 Reasonable triager? | **NO** — known CVE, unconfirmed version, no execution proof |

**Verdict: INVALID**
- **Reason:** Known CVE (CVE-2026-54390); shop version unconfirmed; payload reflected not executed; execution only in backend email renderer (unobservable). Violates "known-vulnerable library without program-specific exploit" rejection criterion.

---

## SUMMARY

| # | Lead | Verdict | CVSS |
|---|------|---------|------|
| 1 | FFN OAuth leaked credentials | **VALID** | 7.5 |
| 2 | FFN OAuth scope escalation | **VALID** | 8.1 |
| 3 | FFN OAuth redirect_uri bypass | **VALID** | 6.8 |
| 4 | FFN API documentation exposure | **HOLD** | — |
| 5 | ERP env-config secrets | **INVALID** | — |
| 6 | Zitadel device flow (rejected by bot) | **INVALID** | — |
| 7 | Cross-tenant BOLA x-tenant-id | **HOLD** | — |
| 8 | OAuth device flow client_id | **HOLD** | — |
| 9 | SSTI bountyshop CVE-2026-54390 | **INVALID** | — |

**Recommended immediate action:** Report Leads 1+2+3 as a single chained finding (credential leak → scope escalation → redirect_uri bypass) to bugs.olivermaicher.eu. Combined impact: attacker with leaked credentials can mint write-scoped JWTs and intercept authorization codes via open redirect.
