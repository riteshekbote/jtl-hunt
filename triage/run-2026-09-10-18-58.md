
===== TRIAGE 2026-09-10 18:58:48 UTC =====
[0m
> build · mimo-v2.5-free
[0m
I need to read the program scope file first, then evaluate the leads. Let me check for the scope file.
[0m✱ [0mGlob "**/jtl.md"[90m 1 match[0m
[0m✱ [0mGlob "**/scope.yml"[90m 1 match[0m
[0m→ [0mRead inventory/jtl.md
[0m→ [0mRead scope.yml
[0m✱ [0mGlob "**/*lead*"[90m 7 matches[0m
[0m✱ [0mGlob "**/leads/**"[90m 7 matches[0m
Let me check if there are lead files in the workspace that need to be evaluated.
[0m✱ [0mGlob "**/*.md"[90m 70 matches[0m
[0m→ [0mRead new-leads.txt
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-nemotron3.md
The user's message ends with "LEADS:" but no leads are listed. The `new-leads.txt` is empty. However, the workspace contains extensive hunt data. Let me extract the distinct, actionable hypotheses from the lead files and inventory to triage.

Based on my review of the lead files, scope, and inventory, here are the unique hypotheses extracted for triage:

---

## TRIAGE: 7-Question Gate Evaluation

### LEAD 1: FFN OAuth Scope Escalation (oauth2.api.jtl-software.com)

| Q | Answer |
|---|--------|
| Q1 In scope? | YES — `*.jtl-software.com` is JTL-owned infrastructure (program scope) |
| Q2 Attacker reachable? | YES — public OAuth2 token endpoint, no auth needed for client_credentials grant |
| Q3 Real security impact? | YES — client registered for `ffn.merchant.read` obtains JWT with `ffn.merchant.write`; server grants any `ffn.*` scope without validation |
| Q4 Provable non-invasively? | YES — POST to `/token` with leaked credentials + requested scope returns JWT with elevated scope; read-only token request |
| Q5 Novel/unreported? | LIKELY YES — OAuth scope validation bypass is a logic flaw, not a CVE |
| Q6 Not on always-rejected list? | YES — not DoS, not info disclosure, not brute-force |
| Q7 Reasonable triager accept? | YES — demonstrates authorization bypass in OAuth flow |

**VERDICT: VALID**
- **Proof steps**: POST `https://oauth2.api.jtl-software.com/token` with `grant_type=client_credentials&client_id=97170e64-d390-4696-ba46-d6fcef8207de&client_secret=f364ldUw3wIJFGn3JXE2NpGdAvUSMlmK72gsYg1z&scope=ffn.merchant.write` — JWT contains `ffn.merchant.write` despite client registered for `ffn.merchant.read` only
- **Impact**: Attacker with leaked creds can obtain write-level access to FFN merchant API; scope escalation enables unauthorized data modification
- **CVSS 3.1**: 8.1 (High) — AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N
- **Channel**: bugs.olivermaicher.eu

---

### LEAD 2: FFN OAuth Credentials Leaked in Public GitHub (kruegge82/jtl-ffn-php-sdk)

| Q | Answer |
|---|--------|
| Q1 In scope? | YES — JTL-owned OAuth client credentials for FFN API |
| Q2 Attacker reachable? | YES — GitHub repo is public; credentials in README |
| Q3 Real security impact? | YES — valid client_id + client_secret enables OAuth token acquisition for FFN API |
| Q4 Provable non-invasively? | YES — publicly visible in GitHub; token exchange is read-only |
| Q5 Novel/unreported? | LIKELY YES — developer credential leak in public repo |
| Q6 Not on always-rejected list? | YES |
| Q7 Reasonable triager accept? | MAYBE — programs sometimes consider leaked credentials "informational" if the creds are for a test/sandbox app. But scope says "all company-owned infrastructure" and these are production creds |

**VERDICT: HOLD**
- **Reason**: Creds may be for a sandbox/test app (`jtl-ffn-php-sdk`). Need to confirm whether the token obtained grants access to production FFN data or only sandbox. If sandbox-only, severity drops to informational. Hold pending scope clarification from program.
- **Proof steps**: Use client_credentials grant to obtain token, then attempt data access on `ffn.api.jtl-software.com` vs `ffn-sbx.api.jtl-software.com`
- **Impact**: If production — HIGH (unauthorized API access); if sandbox — LOW/Informational
- **CVSS 3.1**: 7.5 (High) if production; 3.1 (Low) if sandbox — AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N
- **Channel**: bugs.olivermaicher.eu

---

### LEAD 3: ERP Cloud env-config Leak (erp.jtl-cloud.com)

| Q | Answer |
|---|--------|
| Q1 In scope? | YES — `erp.jtl-cloud.com` is JTL Cloud ERP |
| Q2 Attacker reachable? | YES — env JSON served in public HTML |
| Q3 Real security impact? | UNCLEAR — exposes Sentry DSN, PostHog token, Zitadel client_id, Ory URL. Public client_ids are normal; Sentry DSN/PostHog token are client-side analytics keys |
| Q4 Provable non-invasively? | YES — GET `erp.jtl-cloud.com` returns env JSON in HTML |
| Q5 Novel/unreported? | LIKELY NOT — env variables in frontend JS is a common misconfiguration pattern; Sentry DSNs are designed to be public |
| Q6 Not on always-rejected list? | BORDERLINE — "Disclosure of known public files or directories" and "Descriptive error messages or headers" are out-of-scope |
| Q7 Reasonable triager accept? | NO — Sentry DSNs are public-by-design; PostHog tokens are client-side; public Zitadel client_id is expected; no actual secret exposure |

**VERDICT: INVALID**
- **Reason**: Exposed values (Sentry DSN, PostHog token, public OAuth client_id) are client-side instrumentation keys designed to be public. No actual secret or credential exposure. Falls under "Disclosure of known public files" which is always-rejected.
- **Proof steps**: N/A — informational at best
- **Impact**: None — these are client-side analytics/telemetry keys
- **CVSS 3.1**: N/A
- **Channel**: N/A

---

### LEAD 4: ERP Cross-Tenant BOLA via x-tenant-id (api.jtl-cloud.com)

| Q | Answer |
|---|--------|
| Q1 In scope? | YES — `api.jtl-cloud.com` is JTL Cloud ERP API |
| Q2 Attacker reachable? | PARTIALLY — endpoint returns 401 without JWT; requires valid token to test |
| Q3 Real security impact? | YES (if exploitable) — cross-tenant PII/financial data access |
| Q4 Provable non-invasively? | NO — requires valid JWT for at least one tenant, then testing cross-tenant access. This is invasive testing against live customer data |
| Q5 Novel/unreported? | UNKNOWN — common pattern in multi-tenant SaaS; cannot confirm without testing |
| Q6 Not on always-rejected list? | YES |
| Q7 Reasonable triager accept? | NO — no proof of exploitability; requires active testing against live multi-tenant system; pure hypothesis without evidence |

**VERDICT: INVALID**
- **Reason**: Pure hypothesis with zero proof. Requires valid JWT + cross-tenant testing which is invasive. Cannot be validated passively. No evidence of actual tenant isolation weakness.
- **Proof steps**: None available — would require creating account on one tenant and testing against another (invasive)
- **Impact**: N/A — unproven
- **CVSS 3.1**: N/A
- **Channel**: N/A

---

### LEAD 5: OAuth Device Flow Public Client Token Acquisition (auth.jtl-cloud.com)

| Q | Answer |
|---|--------|
| Q1 In scope? | YES — `auth.jtl-cloud.com` is JTL's OAuth server |
| Q2 Attacker reachable? | PARTIALLY — OIDC discovery returns 200; device/auth endpoint returns 404 (confirmed removed) |
| Q3 Real security impact? | N/A — device flow endpoint is dead (404) |
| Q4 Provable non-invasively? | NO — endpoint removed; cannot test |
| Q5 Novel/unreported? | N/A — endpoint no longer exists |
| Q6 Not on always-rejected list? | YES |
| Q7 Reasonable triager accept? | NO — the vulnerable endpoint (`/oauth2/device/auth`) returns 404; it has been removed/disabled. No current attack surface. |

**VERDICT: INVALID**
- **Reason**: Device authorization endpoint confirmed removed (HTTP 404). OIDC discovery still lists it, but the endpoint itself is dead. Cannot exploit a non-existent endpoint.
- **Proof steps**: `GET https://auth.jtl-cloud.com/oauth2/device/auth` returns 404
- **Impact**: None — endpoint removed
- **CVSS 3.1**: N/A
- **Channel**: N/A

---

### LEAD 6: Docker Test Environment API/OAuth/GraphQL Exposure

| Q | Answer |
|---|--------|
| Q1 In scope? | BORDERLINE — `*.docker.jtl-software.de` are test containers, but program scope says "all company-owned infrastructure" |
| Q2 Attacker reachable? | NO — all 300 hosts resolve to single IP 31.172.91.250; TCP 80/443 timeout. Not internet-accessible |
| Q3 Real security impact? | N/A — hosts unreachable |
| Q4 Provable non-invasively? | NO — cannot reach hosts |
| Q5 Novel/unreported? | N/A — cannot test |
| Q6 Not on always-rejected list? | YES |
| Q7 Reasonable triager accept? | NO — unreachable from internet; no evidence of any vulnerability; pure speculation about test env misconfigurations |

**VERDICT: INVALID**
- **Reason**: All 300 docker.jtl-software.de hosts unreachable from internet (TCP timeout on shared IP 31.172.91.250). Cannot validate any hypothesis. Test environments behind network boundary.
- **Proof steps**: `TCP connect 31.172.91.250:443` → timeout
- **Impact**: None — hosts not internet-accessible
- **CVSS 3.1**: N/A
- **Channel**: N/A

---

### LEAD 7: Bountyshop SSTI via Contact Form (CVE-2026-54390)

| Q | Answer |
|---|--------|
| Q1 In scope? | YES — `bountyshop.jtl-software.com` is the official bug bounty test shop |
| Q2 Attacker reachable? | YES — contact form is public |
| Q3 Real security impact? | YES (if exploitable) — unauthenticated RCE via Smarty template injection |
| Q4 Provable non-invasively? | NO — payload `{7*7}` reflected but NOT executed in response. Actual exploit requires backend email rendering which is not observable |
| Q5 Novel/unreported? | NO — CVE-2026-54390 is a known/public vulnerability |
| Q6 Not on always-rejected list? | BORDERLINE — "Outdated software / library versions" and "Use of known-vulnerable library without program-specific exploit" are out-of-scope |
| Q7 Reasonable triager accept? | NO — known CVE, version unconfirmed (NOVA v=5.0.0 is template version), payload reflected but not executed, requires backend email trigger unobservable |

**VERDICT: INVALID**
- **Reason**: Known public CVE; test shop version unconfirmed (NOVA template v=5.0.0 ≠ shop version); payload reflected but not executed; backend email rendering unobservable; falls under "known-vulnerable library without program-specific exploit" which is always-rejected.
- **Proof steps**: `POST https://bountyshop.jtl-software.com/Kontakt` with `{7*7}` → reflected in form, no execution
- **Impact**: N/A — unproven on this instance
- **CVSS 3.1**: N/A
- **Channel**: N/A

---

## SUMMARY

| # | Lead | Verdict | Key Reason |
|---|------|---------|------------|
| 1 | FFN OAuth Scope Escalation | **VALID** | Confirmed: client_credentials returns JWT with unauthorized scope |
| 2 | FFN OAuth Leaked Credentials | **HOLD** | Creds valid but may be sandbox-only; needs scope clarification |
| 3 | ERP env-config Leak | **INVALID** | Client-side analytics keys (Sentry DSN, PostHog) are public-by-design |
| 4 | ERP Cross-Tenant BOLA | **INVALID** | Pure hypothesis, zero proof, requires invasive testing |
| 5 | OAuth Device Flow | **INVALID** | Endpoint removed (404), no attack surface |
| 6 | Docker Test Env Exposure | **INVALID** | All 300 hosts unreachable from internet |
| 7 | Bountyshop SSTI (CVE-2026-54390) | **INVALID** | Known CVE, version unconfirmed, payload not executed |

**Actionable**: Lead 1 (FFN OAuth Scope Escalation) is ready for submission to bugs.olivermaicher.eu. Lead 2 needs scope clarification before reporting.
