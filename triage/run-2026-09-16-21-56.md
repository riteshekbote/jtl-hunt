
===== TRIAGE 2026-09-16 21:56:13 UTC =====
[0m
> build · mimo-v2.5-free
[0m
I see you've set up a triage workflow for the JTL-Software AG bug bounty program. However, I notice the **LEADS section is empty** - no leads were provided to evaluate.

Let me check if there are scope files in the repository that I should reference:
[0m✱ [0mGlob "**/{scope.yml,scope.yaml,inventory/**,jtl.md}"[90m 2 matches[0m
[0m✱ [0mGlob "**/*.md"[90m 100 matches[0m
[0m→ [0mRead scope.yml
[0m→ [0mRead inventory/jtl.md
[0m→ [0mRead probe-results.md
Let me read the scope file, inventory, and the latest probe results:
[0m→ [0mRead triage/run-2026-09-16-18-55.md
[0m→ [0mRead leads
Let me check the latest triage run and the leads directory to find what needs to be triaged:
[0m→ [0mRead leads/lead-mimo.md
[0m→ [0mRead leads/lead-bigpickle.md
[0m→ [0mRead leads/lead-nemotron3.md
**No leads provided** in the `LEADS:` section. However, I have the full context from the lead files. Let me triage the distinct hypotheses from the lead files against the 7-Question Gate:

---

## TRIAGE — 7-Question Gate Evaluation

---

### LEAD 1: FFN OAuth Credential Leak (GitHub)

**Source:** bigpickle/nemotron — client_id `97170e64-d390-4696-ba46-d6fcef8207de` + client_secret `f364ldUw3wIJFGn3JXE2NpGdAvUSMlmK72gsYg1z` committed to `kruegge82/jtl-ffn-php-sdk` public README

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** — scope.yml covers "all company-owned infrastructure, APIs, cloud services" |
| Q2 Reachable? | **YES** — public GitHub repo, unauthenticated read access confirmed |
| Q3 Real impact? | **YES** — leaked client_secret enables OAuth token acquisition; confirmed scope escalation (read→write) |
| Q4 Provable non-invasively? | **YES** — GET the public GitHub README, extract creds, POST client_credentials to token endpoint |
| Q5 Novel? | **LIKELY YES** — credentials in a developer SDK README is a specific finding; requires checking if JTL already rotated |
| Q6 Always-rejected? | **NO** — not DoS, not outdated lib, not public-only data |
| Q7 Reasonable triager? | **YES** — leaked secret on public repo with confirmed scope escalation is bounty-worthy |

**Verdict: VALID**

- **Proof steps:** (1) GET `https://github.com/kruegge82/jtl-ffn-php-sdk` README, extract client_id + client_secret. (2) POST `https://oauth2.api.jtl-software.com/token` with `grant_type=client_credentials&client_id=<id>&client_secret=<secret>&scope=ffn.merchant.read`. (3) Decode JWT — confirm `ffn.merchant.write` scope present despite only requesting `read`. All read-only.
- **Impact:** Leaked OAuth client_secret for FFN (Fulfillment Network) API with confirmed scope escalation. Attacker can mint valid JWTs and obtain write access to fulfillment operations.
- **CVSS 3.1:** 8.6 (High) — AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:N/A:N (confidentiality + integrity of token scope)
- **Reporting:** bugs.olivermaicher.eu — include GitHub permalink, token endpoint POST response (redact secret in report body, reference commit SHA instead)

---

### LEAD 2: OAuth Scope Escalation (FFN)

**Source:** bigpickle/nemotron — client registered for `ffn.merchant.read` obtains JWT containing `ffn.merchant.write` via `oauth2.api.jtl-software.com`

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** — OAuth API on jtl-software.com |
| Q2 Reachable? | **YES** — token endpoint publicly accessible |
| Q3 Real impact? | **YES** — unauthorized write access to fulfillment operations |
| Q4 Provable non-invasively? | **YES** — POST with leaked creds, decode JWT, show scope mismatch |
| Q5 Novel? | **LIKELY YES** — server grants write scope despite read-only registration |
| Q6 Always-rejected? | **NO** |
| Q7 Reasonable triager? | **YES** — token scope escalation is a real authz defect |

**Verdict: VALID**

- **Proof steps:** (1) POST client_credentials with `scope=ffn.merchant.read`. (2) Decode returned JWT. (3) Show `ffn.merchant.write` present in claims despite only `read` requested. Read-only; no data access.
- **Impact:** Any registered FFN client can escalate to write operations — modify fulfillment orders, inventory, merchant data.
- **CVSS 3.1:** 7.2 (High) — AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:N
- **Reporting:** bugs.olivermaicher.eu

---

### LEAD 3: Silent Scope Degradation (FFN)

**Source:** bigpickle — requesting unauthorized scopes (e.g., `ffn.admin.write`) returns HTTP 200 + token with empty scopes `[]` instead of `invalid_scope` error

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** |
| Q2 Reachable? | **YES** |
| Q3 Real impact? | **NO** — empty scopes `[]` = no access granted; security is maintained |
| Q4 Provable? | Yes but moot |
| Q5 Novel? | N/A — behavior may be intentional (permissive token issuance with empty scopes) |
| Q6 Always-rejected? | Borderline — could be argued as "best practices" |
| Q7 Reasonable triager? | **NO** — no security impact when scopes are empty |

**Verdict: INVALID**

- **Reason:** No impact — token with empty scopes `[]` grants zero access. The server correctly refuses unauthorized scopes; returning HTTP 200 is a design choice, not a vulnerability.

---

### LEAD 4: ERP env-config Leak (Sentry DSN / PostHog token)

**Source:** bigpickle — `erp.jtl-cloud.com` serves `jtl-cloud-env-variables` JSON with Sentry DSN, PostHog token, Zitadel client_id, Ory URL in public HTML

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** |
| Q2 Reachable? | **YES** — public HTML |
| Q3 Real impact? | **UNLIKELY** — Sentry DSN is designed for client-side use (public); PostHog token is client-side analytics; Zitadel client_id is public OIDC client |
| Q4 Provable? | Yes — but no exploit path demonstrated |
| Q5 Novel? | N/A — public client-side config is expected |
| Q6 Always-rejected? | **YES** — "Descriptive error messages or headers (e.g. Stack Traces, banner grabbing)" and "Exposure of known public files" are out-of-scope |
| Q7 Reasonable triager? | **NO** — client-side env vars with public tokens is informational only |

**Verdict: INVALID**

- **Reason:** Out-of-scope per program rules — "exposure of known public files or directories" and client-side configuration values (Sentry DSN, PostHog analytics token, public OIDC client_id) are expected in frontend HTML. No exploitation path without Sentry project auth.

---

### LEAD 5: Cross-Tenant BOLA via x-tenant-id (ERP GraphQL)

**Source:** bigpickle/nemotron — `api.jtl-cloud.com/erp/v2/graphql` with client-supplied `x-tenant-id` header

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** |
| Q2 Reachable? | **PARTIAL** — endpoint returns 401 without JWT; requires authenticated token |
| Q3 Real impact? | **YES** — cross-tenant PII/financial data access if tenant validation relies on header |
| Q4 Provable non-invasively? | **NO** — requires valid JWT for two different tenants; cannot obtain without accounts |
| Q5 Novel? | Architecture suggests possible, but unverified |
| Q6 Always-rejected? | No |
| Q7 Reasonable triager? | **HOLD** — hypothesis is sound but unproven; no evidence of actual tenant isolation failure |

**Verdict: HOLD**

- **Reason:** Valid hypothesis (x-tenant-id header is client-controlled per docs), but requires AUTH_HELPED — cannot prove cross-tenant access without valid tokens from two tenants. A reasonable triager would accept this as a plausible finding but request a PoC with real cross-tenant data access.

---

### LEAD 6: OAuth Device Flow Public Client Token Acquisition

**Source:** nemotron — `auth.jtl-cloud.com` OIDC discovery shows device_authorization_endpoint + public client ("none" auth method) support

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** |
| Q2 Reachable? | **YES** — OIDC discovery confirmed live |
| Q3 Real impact? | **INDETERMINATE** — device flow endpoint was removed (returns 404 per later probe results) |
| Q4 Provable? | **NO** — endpoint no longer live |
| Q5 Novel? | N/A — endpoint disabled |
| Q6 Always-rejected? | No |
| Q7 Reasonable triager? | **INVALID** — attack surface removed since discovery |

**Verdict: INVALID**

- **Reason:** `auth.jtl-cloud.com/oauth2/device/auth` returns HTTP 404 — device authorization endpoint confirmed removed/disabled. No attack surface remains.

---

### LEAD 7: JTL-Shop API Endpoint Exposure (docker.jtl-software.de)

**Source:** bigpickle/nemotron — `1-jtl-shop-a-b-4db87dad.docker.jtl-software.de` with REST API at `/api/v1/`

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** — scope covers "all company-owned infrastructure" |
| Q2 Reachable? | **NO** — all 300 hosts resolve to wildcard IP 31.172.91.250 with TCP timeout; not internet-routable |
| Q3 Real impact? | N/A — unreachable |
| Q4 Provable? | **NO** — TCP timeout prevents any verification |
| Q5 Novel? | N/A |
| Q6 Always-rejected? | No |
| Q7 Reasonable triager? | **INVALID** — unverifiable from internet |

**Verdict: INVALID**

- **Reason:** All docker.jtl-software.de hosts (300 containers) resolve to single wildcard IP with TCP timeout — not internet-accessible. Cannot demonstrate any impact from external network.

---

### LEAD 8: Bountyshop SSTI (CVE-2026-54390)

**Source:** nemotron — contact form at `bountyshop.jtl-software.com/Kontakt` with Smarty template

| Q | Answer |
|---|--------|
| Q1 In scope? | **YES** — official bug bounty test shop |
| Q2 Reachable? | **YES** — HTTP 200 |
| Q3 Real impact? | **POTENTIALLY** — unauthenticated RCE via Smarty SSTI if version 5.2.0–5.7.1 |
| Q4 Provable non-invasively? | **NO** — `{7*7}` payload reflected but not executed in response; requires backend email template rendering (unobservable) |
| Q5 Novel? | **NO** — CVE-2026-54390 is public, NOVA template v=5.0.0 may be below vulnerable range |
| Q6 Always-rejected? | Borderline — public CVE, version unconfirmed, execution unobservable |
| Q7 Reasonable triager? | **NO** — no proof of execution; known public CVE; test shop likely patched |

**Verdict: INVALID**

- **Reason:** (1) NOVA template v=5.0.0 may be below CVE-2026-54390 vulnerable range (5.2.0–5.7.1). (2) SSTI payload `{7*7}` reflected but not executed — no evidence of code execution. (3) Backend email rendering is not observable from outside. (4) Public CVE — test shop likely patched. Cannot prove non-invasively.

---

## Summary

| # | Lead | Verdict | CVSS |
|---|------|---------|------|
| 1 | FFN OAuth Credential Leak (GitHub) | **VALID** | 8.6 |
| 2 | FFN OAuth Scope Escalation | **VALID** | 7.2 |
| 3 | Silent Scope Degradation (empty scopes) | **INVALID** | — |
| 4 | ERP env-config Leak (Sentry/PostHog) | **INVALID** | — |
| 5 | Cross-Tenant BOLA (x-tenant-id) | **HOLD** | — |
| 6 | OAuth Device Flow (removed endpoint) | **INVALID** | — |
| 7 | JTL-Shop docker.jtl.software.de (unreachable) | **INVALID** | — |
| 8 | Bountyshop SSTI (CVE-2026-54390) | **INVALID** | — |

**Actionable:** 2 VALID leads ready for submission to bugs.olivermaicher.eu, 1 HOLD pending token access.
